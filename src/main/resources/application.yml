server:
  port : 8081

spring:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://localhost:5432/db
    #url: jdbc:postgresql://vocabulary-db:5432/db
    username: cartman
    password: cartman
    driverClassName: org.postgresql.Driver
  sql:
    init:
      platform: postgres

logging:
  level:
    org.hibernate.tool.schema: TRACE

